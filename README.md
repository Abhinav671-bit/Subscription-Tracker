# 📦 Subscription Tracker (Backend)

A backend service for managing user subscriptions, built while learning backend development concepts.

## 🚀 Features

* User Authentication (Signup/Login)
* Authorization using middleware
* CRUD operations for subscriptions
* RESTful API structure
* Error handling middleware
* MongoDB integration

## 🛠️ Tech Stack

* Node.js
* Express.js
* MongoDB
* Mongoose
* JWT Authentication

## 📁 Project Structure

* `controllers/` → Handles request logic
* `models/` → Database schemas
* `routes/` → API endpoints
* `middleware/` → Auth & error handling
* `config/` → Configuration files
* `database/` → DB connection

## ⚙️ Setup Instructions

1. Clone the repository
2. Install dependencies:

   ```bash
   npm install
   ```
3. Create a `.env` file and add:

   ```env
   MONGO_URI=your_mongodb_uri
   JWT_SECRET=your_secret
   PORT=5000
   ```
4. Run the server:

   ```bash
   npm start
   ```

## 📌 Status

🚧 Work in Progress — built by following a backend tutorial and actively being improved.

## 🎯 Future Improvements

* Add email notifications
* Subscription reminders
* Deployment (VPS / cloud)
* Better validation & security

## 🤝 Learning Note

This project was created as part of my backend learning journey.
I am continuously improving it by adding new features and refining the code.

---

⭐ If you like it, feel free to give it a star!
